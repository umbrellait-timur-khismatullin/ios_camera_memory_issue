name: ios_camera_memory_issue
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera:
    git:
      url: https://github.com/umbrellait-mikhail-plotnikov/plugins.git
      ref: fix-unexpected-memory-usage-on-iOS
      path: packages/camera/camera
  gallery_saver: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true